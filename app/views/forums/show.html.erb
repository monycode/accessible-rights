
<div class="forum-banner">
  <div class="forum-banner-container">
    <h1 class="forum-banner-title">Forum</h1>
      <%# <div class="forum-title-line"></div> %>
  </div>
</div>

<div class="add-a-new-question-section-container">
  <p> The lawyers of the accesible rights community are here to help!</p>
  <div class="line"></div>
  <h1><%= "Add a new question" %></h1>
</div>
  <br>
<div class="forum-form-container">
  <%= simple_form_for [ @forum, @question ] do |f| %>
    <%= f.input :title, placeholder: "Whats your question?" %>
    <%= f.input :content, placeholder: "Add detailed information about your question" %>
    <p><%= f.submit "Add question", class: "btn btn-primary" %></p>
  <% end %>
</div>



<div class="forum-questions">
  <h1><%= @forum.name %></h1>

  <div class="questions">
    <% @forum.questions.each do |question| %>
    <div class="question-card">
      <div id="question-<%= question.id %>">
        <p><%=link_to question.title, question_path(question) %></p>
        <p><%= question.content %></p>
      </div>
    </div>
    <% end %>
  </div>
</div>
